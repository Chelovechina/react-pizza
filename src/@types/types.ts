export type PizzaType = {
  count?: number
  id: number
  imageUrl: string
  price: number
  sizes: number[]
  title: string
  types: number[]
}

export type SortType = {
  name: string
  sortProperty: string
}

